services:
  - type: web
    name: hiv-regimen-selector-backend
    env: python
    buildCommand: pip install -r requirements.txt && python ml/train.py
    startCommand: python run_app.py
    plan: free
    autoDeploy: true
